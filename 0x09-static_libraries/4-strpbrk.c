#include "main.h"
#include <stdio.h>

/**
 * _atoi - checks if letter is uppercase
 *
 * @s: The value that will be passed
 *
 * Return: 1 if true, 0 if otherwise
 */

char *_strpbrk(char *s, char *accept)
{
}
