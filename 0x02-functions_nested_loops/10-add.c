#include "main.h"

/**
 * add - adds 2 numbers together
 * @n: the int to extract the last digit from
 * Return: value of the last digit
 */
int add(int p, int q)
{
	int i;
	
	i = p + q;
	return (i);
}
