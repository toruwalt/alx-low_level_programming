#include "main.h"
#include <stdio.h>
/**
 * add - adds 2 numbers together
 * @p: first digit to be added
 * @q: second digit to be added
 * Return: value of the last digit
 */
int add(int p, int q)
{
	int i;

	i = p + q;
	return (i);
}
