#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * calloc - A function that allocates memory for an array, using malloc.
 * @nmemb: The number of memebers to be allocated for.
 * @size: Size to be allocated
 * Return: - 0 always
 */

void *_calloc(unsigned int nmemb, unsigned int size)
{
	void *d;

	d = calloc(nmenb, sizeof(
	return (0);
}
